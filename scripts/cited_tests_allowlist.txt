# Allowlist for scripts/check_cited_tests.py
#
# One bare test name (or one `<doc-path> <symbol> <cited-coord>` row) per line;
# `#` starts a comment. A row here is a citation the lint will NOT fail on.
#
# THIS IS A LEDGER, NOT A DUMPING GROUND. Every row states WHY in one line. The
# only legitimate reason is that the citation is deliberately historical: the text
# citing it is a frozen record that is not trued up. A citation that is simply
# wrong gets fixed, never listed.
