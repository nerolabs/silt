# 2026-09-07 — R2.9: the session remainder becomes a DEPOSIT released at anchor expiry (G-6 amended for the delivery lane)

**Context / trigger:** D-R2.9-NODE-HALF-CALLS call 1 (2026-09-06) ratified the refund direction for the
delivery lane's unsettled session remainder; call 1′ (2026-09-07) ratified the two amendments the mechanism's
certification forced — the payee rule M1 and release-at-anchor-expiry M2, the per-identity live-anchor cap
dropped as refuted. Certification:
`/Users/andrewedmond/Claude/claude/silt-reviews/research/research-outcome/R2.9-session-remainder-refund-and-live-anchor-cap-RESEARCH-CERTIFICATION-2026-09-06.md`.

**Evidence (build-immutable #7):**
- The seam: `core/credit/deliveryanchor.go` `CloseDeliverySession` is the only site that accounts the remainder
  (gate `TestBurnIsCountedOnceAtCloseNotPerSettlement`); `core/node/deliverysession.go` `closeDeliverySession`
  deletes the session first and then calls it once.
- The churn construction (cert §4.2): a bearer anchor passed down a chain of fresh keypairs, each opening,
  closing and re-withdrawing, buys unlimited guard slots for zero net credits under release-AT-CLOSE; under
  release-at-EXPIRY the second identity's refund is locked until the anchor leaves the `W+1` window, so the
  chain stops at ⌊stock/f⌋ live entries — the same quantity the burn bounded (T-DEPOSIT, §5.1).
- The payee hazard (cert §3.5–3.6): `acct()` registers an unknown identity and, on an unconfigured faucet,
  hands it the full grant — the RT-RELAY-1 phantom one lane over. The honest fetcher always has an account
  here (it bought the anchor through `ChargePublish` on this ledger).
- The epoch machinery already exists: `advanceEpoch` → `sweepIfEpochAdvanced`, at most once per epoch, against
  the WATERMARK (R-F8-LATCH).
- R2.12's start-up assertion `capacity × (grant/fee) × (W+1) ≤ MaxPaidSerial/4` survives verbatim under M2 and
  is vacuous under release-at-close (cert §4.4, §6.3).

**Options weighed:**
- **(A) Release at close, credit the presenting fetcher through `acct()`.** The form ratified on 2026-09-06.
  REFUTED by the certification on both clauses: the churn fills the guard for free; `acct()` mints a grant.
- **(B) M1 + M2: one pending-refund record per closed session, released to an EXISTING account when the
  anchor's guard entry leaves the window (or at close, whichever is later); no account ⇒ burned and counted;
  the pending table bounded at the guard cap, refuse-never-evict.** Certified; ratified 1′. One new scalar on
  the session (`maxAnchorEpoch`), one bounded table on the ledger, release riding the existing sweep.
- **(C) Keep the burn.** The pin stays unreachable (G-R212-8 §4). Refused by call 1.

**Decision:** (B), gate G-6R-4 written FIRST and proven RED against (A)'s release-at-close before M2 is built.
The idle window stays refuse-until-set: under (B) it is a liveness choice (table-slot occupancy, capital
latency ≤ the window), no longer a burn rate — `R-REAPER-FORFEIT` closes for this lane, stands for relay.

**Pressure-test:**
- *Is a pending-refund record a Don't #3 surface?* Cert §6.2: `{durable fetcher, amount, release epoch}` is
  inside on all three prongs (read by the release, deleted when it fires, server-local); the REFUSED shape is
  a fetcher identity on the guard entry or in the durable store (G-6R-8).
- *Restart.* The table is ephemeral with the sessions (D-FP2-SCOPE); a restart loses pending remainders and
  live sessions' unsettled faces while their guard entries survive durably. Counted, not hidden (G-6R-9);
  `R-DELIVERY-SESSION-EPHEMERAL` stands, bounded by ≤ f per session.
- *What weakens.* Under the burn a fixed credit stock fills the guard ONCE; under M2 the same stock can fill it
  every window — `R-STOCK-RENEWABLE-OCCUPANCY`, bounded by Σ(credits ever granted)/f per window, the honest
  price of a renewable honest budget. Disclosed, not dressed as a closure.
