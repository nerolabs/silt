# 2026-09-06 — R2.9 node half: the delivery session under G-6 as ratified, with the owner-call seams isolated

**Context / trigger:** the ledger half of R2.9 merged (PR #759). The G-R212-8 certification
(`/Users/andrewedmond/.claude/silt-agent-memory/researcher/reviews/research-outcome/R2.9-G-R212-8-delivery-anchor-quantization-RESEARCH-CERTIFICATION-2026-09-06.md`)
certifies the session's SHAPE (ten clauses, §3.1) and REFUTES the composed 64 GiB pin claim while G-6
(remainder burned) stands. G-6 is an owner call. The owner is away and asked for independent work with the
calls raised at the end. This entry records what can be built without pre-empting that call, and how.

**Evidence (build-immutable #7):**
- The certified clauses C1–C10 (cert §3.1) and gates G-λ-8-1…10 (§8); settle-monotone (§6.1); the idle reaper on
  the node's monotonic clock with a coarse stamp (§6.2, §7); receipt v3 with a cumulative count (§6.3).
- The one clause the owner call changes is C9's LAST SENTENCE — "burn at close is `budget − settled`" — and,
  under a refund, a per-identity live-anchor cap at open (§5.1, R-DELIVERY-BURN-PRICES-THE-GUARD). Every other
  clause is identical under burn and refund.
- The ledger seam already isolates it: `core/credit/deliveryanchor.go` `CloseDeliverySession(remaining)` is the
  only site that accounts the remainder (gate `TestRemainderIsAccountedOnceAtCloseNotPerSettlement`).
- The relay lane's node half is the template: `core/node/relayrole.go:357-460` (open), `relaytransport.go:30-124`
  (handlers, settle), `relayrole.go:202-249` (sweep), `relaypay/wire.go` (decode bounds), the `r214` fixtures.
- The witnessed-demand observable today counts one token per receipt (`core/demand/demand.go:397-399`); under
  spend-at-open the token is spent once per session and a session carries many receipts. Neither R2.9
  certification says how `Bank.demand` counts under a cumulative-count receipt (both keep the receipt's "three
  roles", cert 2026-09-04 §2.4; the G-R212-8 cert §7 refutes a per-session per-object map). D-DEMAND's
  cost-to-wash lever is a decision-ledger item, so the counting rule is research-gated.
- The blind PE on the ledger half (`RULING-R2.9-ledger-half-45178ff-2026-09-06.md` "the coupling nobody named")
  lists five landing sites for the refund: the close, the open (per-identity cap), the occupancy paragraph, the
  R2.12 start-up assertion, and the S5 line. Two are node-half sites.

**Options weighed:**
- **(A) Wait for the owner call; build nothing.** Zero rewrite risk. Costs the hours the owner explicitly asked to
  be used, on a build whose shape is certified and whose one open clause is one function.
- **(B) Build the node half under G-6 as ratified, the two owner-call seams isolated behind named functions and
  named residuals.** The close calls `CloseDeliverySession` (burn); the open has no per-identity cap (it is not
  needed under burn — the burn IS the cap). If the owner chooses refund, the rewrite is: the close credits the
  fetcher's account instead of burning (one function, one gate), and the open gains a per-identity live-anchor
  cap (one check, one gate) — after the refund mechanism's own certification. G-λ-8-10 (a zero-settle session
  spends no face) is left OPEN as a named residual: both of its lifts ("record the face at the first verified
  increment" or "return it at close") are the owner's call in disguise.
- **(C) Build under the refund direction now.** Pre-empts a ratified sentence (G-6) and an uncertified mechanism.
  Refused by the research gate.

**Decision:** (B). The demand-observable counting rule is routed to the Researcher as a focused question and
isolated the same way: the bank gains ONE entry point for a session receipt (`Bank.Witness`) whose counting rule is
the seam; it ships counting one unit of witnessed demand per settled receipt naming the object — the reading
closest to today's "one receipt, one unit" — and the certification decides whether that stands, becomes
byte-weighted, or is something else. Witnessed demand is a NEUTRAL observable never read by standing, so the
seam moves no security property while it waits.

**What is built (the certified shape, cert §3.1):**
- C1/C2: `MsgDeliveryOpen` presenting `k = 1` demand-domain anchor, signed by the durable fetcher over
  `M = H("silt/delivery/open/v1" ‖ serverID ‖ uint32BE(k) ‖ serials)`; verified under the server's own keyset in
  the DEMAND domain (B-7: a relay-domain anchor fails); spent all-or-nothing, durable before admission (G-λ-8-9);
  one live session per (server, fetcher).
- C3: the ceiling `D_max = ⌊f/p⌋·U` and `k_max_delivery = ⌈D_max·p/(U·f)⌉ = 1` DERIVED in `cmd/silt` from the face
  (G-λ-8-1); the quantized pin gate (G-λ-8-2).
- C4/C5/C6: receipt v3 = `H("silt/demand/receipt/v3" ‖ handle ‖ M ‖ Object ‖ Server ‖ Fetcher ‖ uint64BE(count))`;
  the count is CUMULATIVE per session; the server settles the DELTA against the REMAINING budget through
  `SettleDelivery`; a lower or equal count pays 0 by arithmetic (G-λ-8-3); the per-object lane reversal is the
  ledger's.
- C7: the session spans objects; nothing closes it but exhaustion, an explicit close, or idleness (G-λ-8-4); no
  object-keyed collection on the session (G-λ-8-7).
- C8: `MsgDeliveryFund` with a fresh anchor, signed over `H("silt/delivery/fund/v1" ‖ serverID ‖ handle ‖ k ‖
  serials)`; one all-or-nothing spend; budget rises by `f`.
- C9: the idle reaper on `n.clock` (monotonic, injected), keyed on the LAST SETTLEMENT stamp stored at the
  reaper's own granularity (coarse, §7), window REFUSE-UNTIL-SET (`-delivery-idle-window`; the S4 precedent) until
  `T_b` is measured (G-λ-8-5); a reaped session forfeits ≤ its unsettled budget (B-13).
- C10: a hard live-session cap in the `relayMaxLiveSessions` shape; refuse at cap, never evict.
- B-9, the anchored half: a session receipt naming no live session is refused. The v2 flat path
  (`MsgDeliveryReceipt`, token spent at redeem) STAYS callable in this PR: nine test files pin red-team scars on
  it (the RT-C3 series), and its retirement re-homes those gates to the open path — its own PR, after the owner
  call. B-11: the S5 affordability line, computed from the constants.
- The `silt swarm receipt` driver and the sim positive arm move to the session flow (two objects, one session);
  a new e2e runs the real withdrawal, open, settle and idle close over TCP.

**Pressure-test:**
- *Does building under burn ship a lane that fails the pin?* The lane is DARK in production (no v5 `IssuerKeyReg`;
  R-DARK-UNTIL-ERA4) and behind `-accept-delivery-receipts`; the failing claim is the composed pin, which the
  daemon's G-λ-3 gate computes by the byte — that gate's own overclaim is `R-PIN-VACUOUS-UNDER-QUANTIZATION`
  (Economist) and is raised with the owner call, not papered over here.
- *Partial acks keep lanes alive longer (PE smaller note).* FIFO pressure on `maxProvisional` rises; eviction
  reverses the mint (A4), so the failure mode is under-pay, never over-pay. Disclosed; no knob moves.
- *Is `Bank.Witness` counting one unit per settled receipt a guess?* It is the conservative reading of the
  unchanged "three roles" and moves no security property; the alternative (byte-weighted demand) is the
  Researcher's to certify. The seam is one function.

**Verdict on the demand seam, same day.** The one-unit-per-receipt reading shipped as the seam is REFUTED; the
certified rule is `demand += settled/p` read from the ledger's first return (never the receipt's count, never
the node's delta), on its own v3 counter with the P3b distinct count as a second surface. Built as certified
(`Bank.Witness`, `WitnessedIncrements`, gates G-DEM-1…8); the published-claim restatement P-SESSION is recorded
under D-DEMAND as PROPOSED. The certification's adjacent finding (the reversal used the raw count) is closed in
the same pass (`TestAckReversalUsesTheBudgetCappedCount`).

**Blind PE on the node half (same day), and the seam it found.** MERGE-AFTER. Its blocker was created by this
build: settle-monotone made the settlement delta payer-chosen, and the ledger half's per-settlement skim floor
(reviewed under settle-once, where it lost ≤ 7 credits once) became a free, total defeat of the escrow funding —
and, composed with the serve-time skim's claw-back, net negative. Same shape as the parity-per-stripe lesson: the
gates held the piece and missed the seam the fix created. The Researcher certified the remedy as a faithful
implementation of the ratified 1/8 (the cumulative floor with the session's counter as `prior`; no owner call);
built with six gates. Items 2–5 folded in. The PE's scope observation stands for the owner: the G-R212-8
certification's residual says it "still blocks R2.9's node half"; this build ships the node half under G-6 with
the seam isolated and the refutation disclosed — ratify that explicitly.

**All five calls RATIFIED 2026-09-06 at the recommendation (D-R2.9-NODE-HALF-CALLS).**

**Owner calls raised (taken as above):** G-6 burn vs refund (+ the per-identity live-anchor cap); the P-SESSION restatement
of D-DEMAND's published property (certified; ratification owed); the idle-window VALUE (after `T_b`); `R-DEFAULT-CHUNK-BOUNTY-ZERO`.
