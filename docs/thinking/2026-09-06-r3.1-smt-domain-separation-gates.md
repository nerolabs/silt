# 2026-09-06 — R3.1: the Researcher refuted the record's completeness; two latent fold defects closed

- **Seat:** BUILDER (overnight, autonomous) · **Branch:** `builder/r3.1-smt-domain-separation-gates` · **Base:** `4ae8083`

**Trigger.** The Rock asked the Researcher to certify the crypto-specialist's 2026-09-01 close design
(prefix scheme + fixed-width leaves ⇒ no type confusion; decline the audit's different-hash advice; V1/V2
gates, already built by the Tester on 2026-09-04). The certification split the answer.

**Evidence, the certification's, re-executed here:** `fold.go:136-140` seeded `(Digest, Preimage)` pairs
unbound; `smt.go` ImportSparseMerkleTrie dispatches on `data[0]` and trusts the store key as digest;
`trie_spec.go` `hashSerialization` — SHA-256 of the bytes for leaf/inner, the EXPANSION root for an
extension preimage (`extension_node.go` `expand()`: one inner node per path bit from bounds[1]-1 down to
bounds[0], placeholder on the off-path side) — which is why a naive "digest == sha256(preimage)" binding
turned three honest fold tests RED before the extension rule was replicated; `node_encoders.go:121-125`
`checkPrefix` panics; grep: no `recover()` in non-test `core/`.

**Built.** G-R31-1 the binding (`foldDigestMismatch`, all three node types); G-R31-2 `proofShapeParsable`
before `VerifyProof` in `Resolve` and the fold; G-R31-3 sum-trie forbid + `sha256.New()` at every SMT site
(the shipped V2 pinned a PROXY: the library calls `NewTrieSpec(h, true)` and `WithValueHasher(nil)` inside
`NewSparseMerkleSumTrie`); G-R31-4 every `tag*` constant ends in exactly one NUL, by source; G-R31-6 the
`go.mod` v1.0.0 pin. The residual record `docs/design/state-root-domain-separation.md` per the cert's §6.3,
cross-linked from `statehash.go` and `consensus-invariants.md`. Positive controls executed: the raw library
call panics on the 33-byte witness (captured with recover in the test); an unbound sibling stalls; a bound
one passes the binding.

**Not built — the owner's:** G-R31-5, `statehash.Root` rejects `len(Value) == 0` (SI-6; a
validity-surface tightening on the committed-state builder; one line). **Not blocking the freeze:** the
certification corrects the routing question — the freeze is a deadline on the Q3 decision only, which is
certified.

**Status:** built; statehash/chain/depcheck suites green; to blind PE code review.
