---
name: vision-research-backlog-triage
description: Triage of docs/VISION.md load-bearing claims into CERTIFIED / named-frontier / un-triaged; the (C) items and their ranking for a parallel research lane
metadata:
  type: project
---

Triaged every load-bearing VISION.md claim (2026-08-27, shaping task, no verdict rendered)
into (A) certified / (B) named-frontier / (C) un-triaged.

**Why:** to open a parallel research lane on highest-leverage longest-lead items without
re-certifying done work.
**How to apply:** when picking research work off VISION, start from the (C) list below; do
not re-open (A)/(B). All five of Andrew's suspected-(C) candidates were already tier (B) —
the privacy build-track (`commission/C-privacy-buildtrack.md`, memo 01) is far deeper than
the candidate list assumed (PIR-at-scale, mixnet-limit, non-colluding-committee all decided
directions with named frontiers).

**The genuine un-triaged (C) items, ranked by lead-time × roadmap-proximity:**
1. **(C-7) Witness-based floor-box validation soundness** — deep; gates the ERA-3 FORMAT
   FREEZE (nearest hard gate). `D-TIERING` ruling 2026-08-27 says it is NOT built (near-term
   bridge holds the tree); opened as Phase-3+ follow-on but soundness argument does not exist.
   Consensus-rule class. **Open this first.**
2. **(C-1) Cold-start capture bound** — deep, zero code dep, gates #183. `m0.md` §10
   "Reachability of maturity" = "No proof; a safe-parameterization, not a theorem." VISION
   107-108 asserts it more strongly than canon proves. Published-claim(M0).
3. **(C-5) Composed operator-economics** (relay+repair credit clears floor-box costs) —
   medium; Phase 4 near-horizon. Economic-mechanism; legs certified, composition not.
4. **(C-2)+(C-3) VISION-vs-canon honesty pass** — short; pre-#183. VISION present-tenses the
   C1 multiplicative interlock (m0.md §3: today `C_honest ≈ D`, bond axis alone) and the
   A-axis "buckets deny massing keys" (m0.md §10: domain self-declared, FREE to evade). Both
   overstate the shipped state vs m0.md's own ⚠ boxes. Certify as target-not-live.

Route to PE+Tester not research: **(C-6)** "bounded-then-fast never OOM" as system-wide
invariant — un-triaged for the keystone SMT/node-store path (ties to the OWED coexistence
test). **(C-4)** curator-accountability (H9, low urgency).

**Tier (B) — the five suspected-(C) that are actually tracked:** γ→1/N sealing boundary
(#182, `m0.md` §10 ⭐); Byzantine size-est under adversarial NodeID placement; PIR-under-churn
(SinglePass frontier, `C-privacy-buildtrack.md` C1); mixnet trilemma limit (memo 01, provable
limit = Das et al. 2018); ZK survivor-Nakamoto ≥ t (`A2-non-globality-metric.md`, #180 built);
strong-form quorum-TTP+threshold-decrypt (`D-DEMAND` P2, blocked on verifiable-escrow/Camenisch-
Shoup no pure-Go); weak-subjectivity checkpoint (`m0.md` §10 F-1 residual 2, mechanized
`-ws-checkpoint`).

Full in-message inventory was the deliverable (not filed as a doc). Related: [[silt-m1-pivot-ordered-roadmap]].
